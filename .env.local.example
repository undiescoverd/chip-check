NEXT_PUBLIC_SUPABASE_URL=                    # client + server
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=        # client, read-only (sb_publishable_...)
SUPABASE_SECRET_KEY=                         # SERVER ONLY — never prefix NEXT_PUBLIC_; used by write Route Handler (sb_secret_...)
STAFF_PIN=                                   # server-side, validated in write Route Handler
NEXT_PUBLIC_READY_TIMEOUT_SECONDS=90         # how long ready orders stay on the display
